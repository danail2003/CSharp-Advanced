using System;

namespace GenericBoxofString
{
    public class StartUp
    {
        static void Main()
        {
            int count = int.Parse(Console.ReadLine());            

            for (int i = 0; i < count; i++)
            {
                string row = Console.ReadLine();

                Box<string> value = new Box<string>();
                value.Data = row;

                Console.WriteLine(value);
            }
        }
    }
}
